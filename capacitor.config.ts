import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.swansonator.personalpower2',
  appName: 'PersonalPowerII',
  webDir: 'dist'
};

export default config;
